import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  // TODO: Define and use scss variables, set overall style defaults (ex: button has no border), and so on.
  styleUrls: ['./app.component.scss'],
  templateUrl: './app.component.html',
})
export class AppComponent {}
